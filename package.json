{
  "name": "proj1",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"npx tsc --watch\" \"npx live-server --port=5500 --watch=dist/*\"",
    "build": "npx tsc"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@swc/cli": "^0.7.3",
    "@swc/core": "^1.11.22",
    "concurrently": "^9.1.2",
    "live-server": "^1.2.2",
    "typescript": "^5.8.3"
  }
}
